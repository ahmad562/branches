name: schedule workflow

  on:
    schedule:
      - cron: "0 12 * * *"

  jobs:
    run-script:
      runs-on: ubuntu-latest

      steps:
      - name: print message
        run: echo "schdeuled for 12 pm "
